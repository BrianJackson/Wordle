# Core runtime dependencies for the Wordle solver
wordfreq
numpy

# Build dependencies for creating executables
pyinstaller

# (Optional) Testing tools
# pytest

# (Optional) If you later re-add the Flask API / frontend proxy
# flask
# flask-cors
